export enum BookableActionType {
    SET_BOOKABLE = '@Bookable/SET_BOOKABLE',
    SET_GROUP = '@Bookable/SET_GROUP',
    TOGGLE_HAS_DETAILS = '@Bookable/TOGGLE_HAS_DETAILS',
    NEXT_BOOKABLE = '@Bookable/NEXT_BOOKABLE',
}